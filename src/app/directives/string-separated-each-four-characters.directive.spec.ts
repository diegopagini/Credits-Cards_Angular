import { StringSeparatedEachFourCharactersDirective } from './string-separated-each-four-characters.directive';

describe('StringSeparatedEachFourCharactersDirective', () => {
  it('should create an instance', () => {
    const directive = new StringSeparatedEachFourCharactersDirective();
    expect(directive).toBeTruthy();
  });
});
